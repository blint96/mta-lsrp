local screenX, screenY = guiGetScreenSize()
local baseX, baseY = 1366, 768

--[[function shaderMenu ()
	-- BG
	--dxDrawImage ( (300 / baseX) * screenX, (250 / baseY) * screenY, (750 / baseX) * screenX, (300 / baseY) * screenY, "ui-background.png")

	-- Text
	-- Test zmian 2 SFTP
	--local font = dxCreateFont("calibri.ttf", 20)
	--dxDrawText("Personalizacja dodatków graficznych", (340 / baseX) * screenX, (280 / baseY) * screenY, (340 / baseX) * screenX, 100, tocolor(200, 200, 200), 1, font)
end
addEventHandler ( "onClientRender", root, shaderMenu )]]--
